#!/usr/bin/env python2.7

import sys

if len(sys.argv) == 1
	sys.argv.append('-')

for path in sys.argv[1:]:
	if path == '-':
		stream = sys.stdin
	else
		stream = open(path)
	for line in open(path):
		print line,
